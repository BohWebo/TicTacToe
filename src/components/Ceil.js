import React from 'react';


const Ceil = (props) => (
        <span 
          className="Ceil" 
          data-index={props.index}
        >
          {props.value}
        </span>
);

export default Ceil;